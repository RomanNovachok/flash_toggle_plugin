# flash_toggle_plugin

Local Flutter plugin for Android with static Dart API:

- `FlashTogglePlugin.toggleLight()` toggles flashlight on/off
- `FlashTogglePlugin.batteryLevel` returns current battery level in percent
